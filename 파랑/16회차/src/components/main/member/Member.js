const Member = ({ editable, time, progress, timerSet, on }) => {
  return `
        <section class="management-wrap">
          <div class="buttons">
            <button class="offset">Management Member</button>
          </div>
        </section>
        `;
};

export default Member;
